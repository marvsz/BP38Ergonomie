#ifndef MINIMIZEDTIMERVIEW_H
#define MINIMIZEDTIMERVIEW_H

#include <QWidget>

class minimizedTimerView : public QWidget
{
    Q_OBJECT
public:
    explicit minimizedTimerView(QWidget *parent = 0);
    ~minimizedTimerView();

signals:

public slots:
};

#endif // MINIMIZEDTIMERVIEW_H
